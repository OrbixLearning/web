import { LearningPathGenerationStatusPipe } from './learning-path-generation-status.pipe';

describe('LearningPathGenerationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new LearningPathGenerationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
