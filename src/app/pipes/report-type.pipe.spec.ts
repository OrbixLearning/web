import { ReportTypePipe } from './report-type.pipe';

describe('ReportTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ReportTypePipe();
    expect(pipe).toBeTruthy();
  });
});
