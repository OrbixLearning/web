import { AudioVoicePipe } from './audio-voice.pipe';

describe('AudioVoicePipe', () => {
  it('create an instance', () => {
    const pipe = new AudioVoicePipe();
    expect(pipe).toBeTruthy();
  });
});
