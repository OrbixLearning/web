import { LearningPathTypePipe } from './learning-path-type.pipe';

describe('LearningPathTypePipe', () => {
  it('create an instance', () => {
    const pipe = new LearningPathTypePipe();
    expect(pipe).toBeTruthy();
  });
});
