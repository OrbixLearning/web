import { DocumentAIUploadStatusPipe } from './document-aiupload-status.pipe';

describe('DocumentAIUploadStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DocumentAIUploadStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
