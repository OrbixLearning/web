import { InstitutionRolePipe } from './institution-role.pipe';

describe('InstitutionRolePipe', () => {
  it('create an instance', () => {
    const pipe = new InstitutionRolePipe();
    expect(pipe).toBeTruthy();
  });
});
